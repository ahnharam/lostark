# 로스트아크 프로젝트 - 컴포넌트 수정 요약

## 수정 개요
SVG 와이어프레임 기반으로 3개의 Vue 컴포넌트를 업데이트하여 디자인 시스템과 UX 요구사항을 반영했습니다.

## 1. EquipmentDetailModal.vue (960×800 모달)

### 주요 변경사항
- **크기**: 960×800px 고정 (반응형 지원)
- **헤더**: 캐릭터명/직업/레벨 + 닫기 버튼
- **탭 구조**: 장비 / 각인 / 세트효과 (3개 탭)
- **레이아웃**: 좌우 2단 그리드 (장비 탭)
  - 좌측: 슬롯/아이콘/베이스스펙
  - 우측: 세부 옵션/품질/세트/트포

### Props 추가
```typescript
interface Props {
  equipment: Equipment
  characterName?: string      // 새로 추가
  characterClass?: string      // 새로 추가
  characterLevel?: string | number  // 새로 추가
  showRawData?: boolean
}
```

### 특징
- 탭 전환 시 부드러운 애니메이션
- 각 탭별 독립적인 스크롤
- 데이터 없을 시 빈 상태 표시
- 모바일: 1단 레이아웃으로 전환

---

## 2. EngravingCard.vue (320px 결과 카드)

### 주요 변경사항
- **너비**: 320px 고정 (모바일 100%)
- **이미지**: 176px 고정 높이
- **텍스트**: 이름(16px Bold), 서브(12px)
- **배지**: Pill 형태 (Radius=999)
- **구조**: Vertical / Gap 12 / Padding 16 / Radius 14

### 레이아웃
```
┌──────────────────────────┐
│   이미지 (176px)          │ ← W=Fill, H=176 Fixed
│   + 레벨 배지             │
├──────────────────────────┤
│ 이름 (16px Bold)          │
│ 직업 • iLv (12px)         │
│ [배지] [배지] [배지]      │ ← Pill 형태 (Gap 8)
└──────────────────────────┘
```

### Props 추가
```typescript
interface Engraving {
  name: string
  icon: string
  description: string
  class?: string          // 새로 추가
  itemLevel?: string | number  // 새로 추가
  badges?: string[]       // 새로 추가
}
```

---

## 3. AutocompleteInput.vue (680px 검색)

### 주요 변경사항
- **크기**: W=680px(최대), H=52px
- **스타일**: Radius=999 (완전한 라운드)
- **구조**: 아이콘(좌) + 입력(중앙, Fill) + Clear(우, Hug)
- **추천어**: 하단에 1줄 Chip 형태 표시

### 레이아웃
```
┌─────────────────────────────────────────┐
│ 🔍  [입력 필드.....................]  ✕ │ H=52
└─────────────────────────────────────────┘
  추천: [예시1] [예시2] [예시3]
  
  ┌─────────────────────────────┐
  │ ⭐ 즐겨찾기                 │
  ├─────────────────────────────┤
  │ • 항목 1                    │
  │ • 항목 2                    │
  └─────────────────────────────┘
```

### Props 추가
```typescript
interface Props {
  modelValue: string
  suggestions: Suggestion[]
  recommendations?: string[]  // 새로 추가 - 추천어 배열
  placeholder?: string
  minChars?: number
  maxSuggestions?: number
}
```

---

## 상태 흐름 통합

### 1. Idle (대기)
- 헤더 + 검색 입력(680px) 노출
- 추천어 1줄 표시

### 2. Typing (입력 중)
- 디바운스 300ms
- 자동완성 드롭다운 표시

### 3. Submit (검색)
- Loading 상태
- 입력/버튼 비활성화

### 4. Results (결과 표시)
- EngravingCard 그리드 (5×2)
- 320px 고정 너비 카드

### 5. Card → Modal (상세 보기)
- 960×800 중앙 오버레이
- 3개 탭으로 정보 분리
- 스크롤 가능

---

## 기술적 고려사항

### CSS 변수 활용
```css
--modal-overlay: rgba(0, 0, 0, 0.7);
--modal-bg: #ffffff (light) / #1a1a1a (dark);
--card-bg: #f5f5f5 (light) / #2a2a2a (dark);
--primary-color: #667eea;
--border-color: #e0e0e0 (light) / #3a3a3a (dark);
```

### 반응형 브레이크포인트
- Desktop: 1024px 이상 (full layout)
- Tablet: 768px~1023px (조정된 레이아웃)
- Mobile: 640px 이하 (1단 레이아웃)

### 애니메이션
- 모달: fadeIn + slideUp (0.3s)
- 드롭다운: slideDown (0.2s)
- 호버: transform + shadow (0.2s~0.3s)

---

## 다음 단계

1. ✅ **기존 파일 백업** 후 교체 (완료)
2. **타입 정의** 추가/수정 (`types/` 디렉토리)
3. **API 연동** 시 새 Props 데이터 전달
4. **테마 변수** 확인 (`theme.css`)
5. **E2E 테스트** 실행

---

## 파일 위치

```
frontend/src/components/common/
├── EquipmentDetailModal.vue  ✅ 수정 완료
├── EngravingCard.vue          ✅ 수정 완료
└── AutocompleteInput.vue      ✅ 수정 완료
```

---

생성일: 2025-11-07
작성자: Claude (Anthropic)
