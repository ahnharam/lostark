# .env.example 파일 - 이 파일을 복사해서 .env 파일을 만드세요

# 데이터베이스 설정
DB_ROOT_PASSWORD=lostark-root-2024
DB_USER=lostark_user
DB_PASSWORD=lostark-pass-2024
# (Railway/FreeDB에서는 실제 호스트와 자격 증명을 플랫폼 환경 변수로 설정하세요)

# 로스트아크 API 키 (https://developer-lostark.game.onstove.com/ 에서 발급)
LOSTARK_API_KEY=your_api_key_here

# 디스코드 토큰
DISCORD_BOT_TOKEN=your_discord_token

# Discord OAuth (웹 로그인/유저 동기화)
# Discord Developer Portal에서 OAuth2 -> General에서 Client ID/Secret 발급 후 입력
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret

# 프론트엔드 URL (로그인 성공 후 리다이렉트)
FRONTEND_BASE_URL=http://localhost:5173

# (선택) 로그인 성공 후 이동 경로 (기본: /raid-schedule)
FRONTEND_LOGIN_REDIRECT_PATH=/raid-schedule

# (선택) CORS 허용 Origin 목록 (콤마 구분)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8081

# 프론트엔드 API 기본 URL (필요 시 수정)
VITE_API_BASE_URL=http://localhost:8080/api
POSTMAN_API_KEY=your_postman_api_here
